server:
  port: 8761

spring:
  application:
    name: eureka-server

eureka:
  client:
    register-with-eureka: false # 서버 본인을 클라이언트로 등록하지 않음
    fetch-registry: false
  instance:
    hostname: localhost